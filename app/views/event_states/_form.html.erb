<%= form_for(@event_state) do |f| %>
  <% if @event_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_state.errors.count, "error") %> prohibited this event_state from being saved:</h2>

      <ul>
      <% @event_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
